
&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	Сообщения.Параметры.УстановитьЗначениеПараметра("Регламент", Элемент.ТекущаяСтрока);
	Если ЗначениеЗаполнено(Элемент.ТекущаяСтрока) Тогда
		ОповеститьОбИзменении(Элемент.ТекущаяСтрока);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	Если ИмяСобытия = "КнопкаВыполнитьСейчас" Тогда 
		Элементы.Сообщения.Обновить();		
	КонецЕсли;                      
КонецПроцедуры
