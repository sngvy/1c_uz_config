
Функция ВыполнитьПроцедуру(ПроцедураСкоринга, Таблица) Экспорт
	Попытка
		Выполнить(ПроцедураСкоринга);
		Результат = Истина;
	Исключение
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = ОписаниеОшибки();
		Сообщение.Сообщить();
		Результат = Ложь;
	КонецПопытки;
	Возврат Результат;
КонецФункции

Функция ПолучитьНастройки(Владелец, УникальныйИдентификатор = Неопределено) Экспорт

	Если УникальныйИдентификатор = Неопределено Тогда
	
		УникальныйИдентификатор = Новый УникальныйИдентификатор();
	
	КонецЕсли;
	
	// TODO - Перенести в Управление СКД
	СКД = ПолучитьМакет("Макет");
	СКД.НаборыДанных.Получить(0).Запрос = Владелец.ЗапросМакета;
	АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(СКД, УникальныйИдентификатор);
	ИсточникНастроек = Новый ИсточникДоступныхНастроекКомпоновкиДанных(АдресВоВременномХранилище);

	Настройки = Новый Структура;
	Настройки.Вставить("Источник", ИсточникНастроек);
	Настройки.Вставить("ПоУмолчанию", СКД.НастройкиПоУмолчанию);
	
	Возврат Настройки;

КонецФункции // ()

