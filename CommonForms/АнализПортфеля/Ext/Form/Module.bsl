
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ЭтаФорма.ПроцентСбора < 50 Тогда
		ЭтаФорма.Элементы.ПроцентСбора.ЦветТекста = WebЦвета.Красный;
	Иначе
		ЭтаФорма.Элементы.ПроцентСбора.ЦветТекста = WebЦвета.Зеленый;
	КонецЕсли;
КонецПроцедуры
