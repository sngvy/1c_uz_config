
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ЭтаФорма.ПроцентСбораОтПортфеля < 50 Тогда
		ЭтаФорма.Элементы.ПроцентСбораОтПортфеля.ЦветТекста = WebЦвета.Красный;
	Иначе
		ЭтаФорма.Элементы.ПроцентСбораОтПортфеля.ЦветТекста = WebЦвета.Зеленый;
	КонецЕсли;
КонецПроцедуры
