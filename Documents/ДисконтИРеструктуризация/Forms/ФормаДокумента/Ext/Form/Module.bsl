
&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(ЭтаФорма.Объект.СрокПогашения) = Ложь Тогда
		Предупреждение("Не задан срок погашения!");
		Отказ = Истина;
	ИначеЕсли ЗначениеЗаполнено(ЭтаФорма.Объект.ЕжемесячныйПлатеж) = Ложь Тогда
		Предупреждение("Не задан ежемесячный платеж!");
		Отказ = Истина;	
	КонецЕсли
КонецПроцедуры