
&НаКлиенте
Процедура ИнформацияОДеле(Команда)
	СтруктураЗаписи = Новый Структура("Должник", Запись.Должник);
	МассивСтруктураЗаписи = Новый Массив;
	МассивСтруктураЗаписи.Добавить(СтруктураЗаписи);
	КлючЗаписи = Новый("РегистрСведенийКлючЗаписи.ДелаОБанкротстве", МассивСтруктураЗаписи);                              
	Параметрыформы = Новый Структура("Ключ", КлючЗаписи);                            
	ОткрытьФорму("РегистрСведений.ДелаОБанкротстве.ФормаЗаписи", Параметрыформы);
КонецПроцедуры
