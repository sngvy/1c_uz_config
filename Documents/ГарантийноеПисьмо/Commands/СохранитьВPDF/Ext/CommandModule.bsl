
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//{{_КОНСТРУКТОР_ПЕЧАТИ(Печать)
	ТабДок = Новый ТабличныйДокумент;
	Печать(ТабДок, ПараметрКоманды);

	ТабДок.ОтображатьСетку = Ложь;
	ТабДок.Защита = Истина;
	ТабДок.ТолькоПросмотр = Ложь;
	ТабДок.ОтображатьЗаголовки = Ложь;
	Shell = Новый COMОбъект("WScript.Shell");
	USERPROFILE = Shell.ExpandEnvironmentStrings("%USERPROFILE%");
	ПоказатьОповещениеПользователя("Документооборот",,"Файл сохранен в папку Документы",БиблиотекаКартинок.NC_ПодсистемаДокументооборот);
	ТабДок.Записать(USERPROFILE + "\Documents\Гарантийное письмо.pdf", ТипФайлаТабличногоДокумента.PDF);
	//}}
КонецПроцедуры

&НаСервере
Процедура Печать(ТабДок, ПараметрКоманды)
	Документы.ГарантийноеПисьмо.Печать(ТабДок, ПараметрКоманды);
КонецПроцедуры
