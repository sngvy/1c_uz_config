
Процедура ОбработкаПроведения(Отказ, Режим)

	// регистр СведенияОСчетах
	Движения.СведенияОСчетах.Записывать = Истина;
	Для Каждого ТекСтрокаСведения Из Сведения Цикл
		Движение = Движения.СведенияОСчетах.Добавить();
		Движение.Период = Дата;
		Движение.Счет = ТекСтрокаСведения.Счет;
		Движение.Свойство = ТекСтрокаСведения.Свойство;
		Движение.Значение = ТекСтрокаСведения.Значение;
	КонецЦикла;

КонецПроцедуры
