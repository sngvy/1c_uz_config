
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//СброситьОчередь(ПараметрКоманды);
	ФоновыеЗаданияМодуль.СинхронизацияСУЗ(ПараметрКоманды);
	Оповестить("КнопкаВыполнитьСейчас", , ПараметрыВыполненияКоманды.Источник);	
КонецПроцедуры

&НаСервере
Процедура СброситьОчередь(Ссылка)
	Набор = РегистрыСведений.РегламентныеДействияПоКатегориям.СоздатьМенеджерЗаписи();
	Набор.РегламентноеДействие = Ссылка;
	Набор.Прочитать();
	Если Набор.РегламентноеДействие.Пустая() Тогда
		Попытка
			Набор.Удалить();
		Исключение
			Сообщить(ОписаниеОшибки());
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры
