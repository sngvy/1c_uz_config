
&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(ЭтаФорма.Объект.Состояние) = Ложь Тогда
		Предупреждение("Не задано состояние документа!",,"Ошибка проведения документа");
		Отказ = Истина;
	ИначеЕсли ЗначениеЗаполнено(ЭтаФорма.Объект.ПланируемаяДатаПлатежа) = Ложь Тогда
		Предупреждение("Не задана планируемая дата платежа!",,"Ошибка проведения документа");
		Отказ = Истина;
	ИначеЕсли ЭтаФорма.Объект.Проведен = Ложь Тогда
		Предупреждение("Документ не проведен!",,"Ошибка записи документа");
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры