
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		
		Возврат;
		
	КонецЕсли;
	
	Если ЭтоНовый() И ЭтотОбъект.НаПоддержке Тогда
	
		Отказ = Истина;
		ВызватьИсключение "Нельзя создавать маркеры с признаком НаПоддержке";
		
	ИначеЕсли Не ЭтоНовый() и Ссылка.НаПоддержке Тогда
		
		Отказ = Истина;
		ВызватьИсключение "Нельзя менять маркер с признаком НаПоддержке";
		
	КонецЕсли;
	
КонецПроцедуры
