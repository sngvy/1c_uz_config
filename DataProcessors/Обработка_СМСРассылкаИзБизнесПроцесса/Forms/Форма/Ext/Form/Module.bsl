
&НаКлиенте
Процедура Отладка(Команда)
	ОтладкаСервер();
КонецПроцедуры

&НаСервере
Процедура ОтладкаСервер()
	НаборСостояние = Новый Структура("Сообщение", "");
    Об = РеквизитФормыВЗначение("Объект");
	Об.РегламентноеЗадание(НаборСостояние);
	Сообщить(НаборСостояние.Сообщение);
КонецПроцедуры
