
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	ПолучитьНовыеСообщения(ПараметрКоманды);
	
	Сообщение = Новый СообщениеПользователю;
	Сообщение.Текст = "Операция закончена";
	Сообщение.Сообщить();

КонецПроцедуры

&НаСервере
Функция ПолучитьНовыеСообщения(НастройкаТелеграмм)

	Обработки.СохранениеИсторииЧатов
		.ПолучитьНовыеСообщения(НастройкаТелеграмм);

КонецФункции // ()
