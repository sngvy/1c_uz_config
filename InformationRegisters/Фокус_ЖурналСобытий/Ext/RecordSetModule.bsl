
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		
		Возврат;
		
	КонецЕсли;
	
	Если ЭтотОбъект.Количество() = 0 Тогда//Это удаление. Защитимся от "случайного" удаления
		
		Отказ = Истина;
		ВызватьИсключение "Нельзя очищать регистр ЖурналСобытий";
		
	КонецЕсли;
	
КонецПроцедуры
