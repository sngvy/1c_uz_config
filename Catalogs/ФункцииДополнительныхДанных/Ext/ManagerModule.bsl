
Функция ВычислитьФункцию(Знач Запрос, Знач МероприятиеТекущее, Знач Значение) Экспорт

	НовоеЗначение = Неопределено;
	Попытка
		Выполнить(Запрос);
		НовоеЗначение = Значение;
	Исключение
		Возврат Значение;
	КонецПопытки;
	Возврат НовоеЗначение;

КонецФункции
