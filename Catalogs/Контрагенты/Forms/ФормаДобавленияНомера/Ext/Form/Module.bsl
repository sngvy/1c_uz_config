
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Должник = ВладелецФормы.Объект.Ссылка;
КонецПроцедуры


&НаКлиенте
Процедура Добавить(Команда)
	Если Не ЗначениеЗаполнено(ВидТелефона) ИЛИ Не ЗначениеЗаполнено(Номер) Тогда	
    	Сообщить("Сначала заполните обязательные реквизиты!");		
	 	Возврат;
	КонецЕсли; 
	
	Строка = ВладелецФормы.Объект.Телефоны.Добавить();
	Строка.ВидТелефона = ВидТелефона;
	Строка.Номер = Номер;
	Строка.КонтактноеЛицо = КонтактноеЛицо;
	Строка.Комментарий = Комментарий;
	
	ВладелецФормы.Модифицированность = Истина;
	ЭтаФорма.Закрыть();
КонецПроцедуры

