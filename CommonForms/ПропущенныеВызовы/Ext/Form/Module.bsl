
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	НачалоПериода = НачалоДня(ТекущаяДата()) - 7 * 3600;
	КонецПериода = НачалоДня(ТекущаяДата()) + 8 * 3600;
	ПропущенныеВызовы.Параметры.УстановитьЗначениеПараметра("НачалоПериода", НачалоПериода);
	ПропущенныеВызовы.Параметры.УстановитьЗначениеПараметра("КонецПериода", КонецПериода);
КонецПроцедуры
